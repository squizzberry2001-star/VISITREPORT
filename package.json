{
  "name": "bestie-visit-cloudflare-d1-sync",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "@netlify/blobs": "^10.7.4",
    "convex": "^1.37.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@netlify/functions": "^3.0.0",
    "tailwindcss": "^4.3.3"
  },
  "scripts": {
    "cloudflare:login": "wrangler login",
    "cloudflare:schema": "wrangler d1 execute visitreport_rbv --remote --file=cloudflare/schema.sql",
    "cloudflare:deploy": "wrangler deploy"
  }
}
